# alpaca-data Semantics

No dedicated `alpaca-data` semantics spec is frozen yet.

Add semantic contracts here when feed selection, cache freshness, snapshot ordering, or batch-fetch behavior needs a canonical written definition.
