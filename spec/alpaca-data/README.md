# alpaca-data Spec

This directory is reserved for stable `alpaca-data` contracts.

The crate currently provides raw market-data clients, cache helpers, and feed-selection helpers. Add crate-specific API, model, and semantics documents here as its public facade and cache contracts solidify.
