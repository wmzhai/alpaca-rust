# alpaca-mock Spec

This directory is reserved for stable `alpaca-mock` contracts.

The crate currently provides Alpaca-compatible mock services and fixtures for local development and verification. Add crate-specific API, model, and semantics documents here as its supported scenarios are formalized.
