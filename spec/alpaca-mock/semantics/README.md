# alpaca-mock Semantics

No dedicated `alpaca-mock` semantics spec is frozen yet.

Add semantic contracts here when scenario coverage, deterministic replay, or mock-behavior guarantees need a written canonical reference.
