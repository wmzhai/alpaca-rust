# alpaca-mock API

No dedicated `alpaca-mock` API spec is frozen yet.

Add canonical mock-service and fixture-serving API references here once the supported testing and local-development surface is formalized.
