# alpaca-http Models

No dedicated `alpaca-http` model spec is frozen yet.

Add shared request, response, transport-config, or retry-state structures here when they need explicit field-level documentation.
