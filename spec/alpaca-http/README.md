# alpaca-http Spec

This directory is reserved for stable `alpaca-http` contracts.

The crate currently owns HTTP transport, authentication, retries, and related wiring. Add crate-specific API, model, and semantics documents here when those boundaries need written canonical references.
