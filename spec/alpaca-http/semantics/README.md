# alpaca-http Semantics

No dedicated `alpaca-http` semantics spec is frozen yet.

Add semantic contracts here when authentication, retry, timeout, concurrency, or transport behavior needs a canonical written reference.
