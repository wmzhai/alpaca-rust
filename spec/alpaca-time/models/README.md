# alpaca-time Models

`alpaca-time` currently defines its shared structural types directly inside `../api/public-api.md`.

Add a dedicated model document here if the crate later needs larger, field-level model references that deserve a standalone file.
