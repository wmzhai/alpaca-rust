# alpaca-core Spec

This directory is reserved for stable `alpaca-core` contracts.

The crate currently provides low-level shared types and helpers. Add crate-specific API, model, and semantics documents here once those contracts need long-lived written guarantees.
