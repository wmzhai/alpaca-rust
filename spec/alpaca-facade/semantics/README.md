# alpaca-facade Semantics

`alpaca-facade` currently captures its boundary semantics inside `../api/alpaca-adapter-api.md`.

Add dedicated semantic documents here if facade-layer contracts expand beyond the current adapter mapping and enrichment rules.
