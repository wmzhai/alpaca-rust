# alpaca-facade Models

`alpaca-facade` currently documents its shared adapter-facing structures inside `../api/alpaca-adapter-api.md`.

Add standalone model references here when facade-specific shared structures grow beyond the current adapter contract.
