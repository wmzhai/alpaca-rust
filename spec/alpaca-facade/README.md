# alpaca-facade Spec

This directory defines the high-level Alpaca integration contracts for `alpaca-facade`.

It is the new home for the legacy adapter-facing spec that used to live alongside the option core spec, and it documents:

- provider-payload to core-model mapping rules
- live enrichment boundaries
- facade-facing shared adapter models
