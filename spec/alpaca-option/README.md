# alpaca-option Spec

This directory defines the provider-neutral option core contract for `alpaca-option`.

It is the new home for the migrated legacy option-core spec content, including:

- canonical public API definitions
- shared option models
- core option semantics
- mirrored internal math-kernel contracts
